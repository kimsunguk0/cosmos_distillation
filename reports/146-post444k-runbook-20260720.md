# Report 146: Post-444K Runbook (register before completion)

Date: 2026-07-20
Status: registered / pending 444K completion (~96% at registration)
Trigger: run when `outputs/reports/stepb_ceonly_444k/ceonly_444k_20260718/fullft_lr3e5_ceonly_444k_e1_summary.json` exists.

Checkpoints in play:
- 444K CE (candidate): `outputs/checkpoints/stepb_ceonly_444k/ceonly_444k_20260718/fullft_lr3e5_ceonly_444k_e1/best_decode`
- 200K CE FullFT: `outputs/checkpoints/stepb_200k_double_promotion/double200k_20260711_181751/fullft_lr3e5_200k_e1/best_decode`
- 200K CE+KD FullFT: `outputs/checkpoints/stepb_r1kd_200k/r1kd_200k_20260717/fullft_lr3e5_r1kd_200k_e1/best_decode`
- 200K CE LoRA: `outputs/checkpoints/stepb_200k_double_promotion/double200k_20260711_181751/lprime_lora_lr2e4_200k_e1/best_decode`
- 20K CE (R0-F): `outputs/checkpoints/stepb_followup/followup_20260711_023134/r0_f_fullft_lr3e5_20k/best_decode`

Common: val512 `data/corpus/val512_seed42_selected_for_10b_fullft_lora_greedy.jsonl`; teacher ref `outputs/references/backbone_eval_v1/frozen_val512_teacher_greedy_ref_v1/rows.jsonl`.

---

## ① Frozen val512 full package (444K) + scaling curve + CI

Run on 444K best_decode:
- P-G vs teacher_ref: `70_eval_checkpoint_free_run.py` --reference-id teacher_greedy_ref_v1 (do_sample false)
- P-G vs GT: `70_...` --geometry-reference gt (or GT ref)
- P-S6 minADE6: `25_decode_checkpoint_overlays.py` --geometry-reference gt --samples-per-row 6 --seed 42
- matched argmax: `82_eval_test_b_teacher_forced.py` (--save-token-sequences) → `audit_val512_matched_argmax_vs_teacher_top1.py`

Then paired CI (5000 bootstrap, seed 42) 444K vs 200K CE via `stepb_compare_decode_summaries.py` (+ minADE6 per-sample CI).

Deliverable: **scaling curve 3 points 20K / 200K / 444K** (greedy ADE/FDE, minADE6, distribution) on identical val512 protocol → final verdict on "data is the bottleneck". Also report **per-scene-category** ADE (val is natural distribution; balanced-200K may win rare cats — see Report 145 §6).

## ② CoT / forgetting health gate (re-run — 54K steps = 2× of 25K)

- VQA/CoT retention: `stepa_eval_q2_vqa_checkpoint.py` on 444K best_decode vs the Step-A init and vs 200K CE (did longer FT forget Step-A VQA capability?).
- **black-delta** (visual grounding): `25_decode_checkpoint_overlays.py --image-ablation black` vs `--image-ablation normal`; black-delta = ADE(black) − ADE(normal). Prior value +1.37. **Observe if it shrank** (shrink = less vision reliance = grounding forgetting → gate concern). Report for 444K, and 200K CE for reference.

## A. Decode-temperature sweep (444K) — requested

Two sampling regimes, each report ADE / FDE / minADE6@6.4s:
- **T=1.0, top-p 1.0** (current ps6 default): `25_...` --temperature 1.0 --top-p 1.0 --samples-per-row 6.
- **T=0.6, top-p 0.98** (deployment-like, matches on_policy_gkd gen cfg): `25_...` --temperature 0.6 --top-p 0.98 --samples-per-row 6.
- Plus true greedy single-path ADE/FDE: `70_...` --no-do-sample.
(Do the same two-regime sweep for the comparison ckpts if a like-for-like table is wanted.)

## B + C. Hidden-state & KV representation (NOT lm-head) — 3 ckpts

Ckpts: **200K CE FullFT, 200K CE+KD FullFT, 200K CE LoRA** (+ teacher cache as anchor).
Teacher anchor: `teacher_cache` `final_lm_pre_head`, traj_body_128, (128, 4096) per sample, all val512 present.

Extract per ckpt (teacher-forced, val512, traj-body positions):
- **Hidden-state alignment vs teacher**: `21_probe_hidden_latent.py` — cosine, gram_corr, **centered_gram_corr (CKA)** (dimension-agnostic: student 2B vs teacher 4096). Also pairwise CE-vs-KD and CE-vs-LoRA hidden deltas (Δ representation).
- **Prefill / KV QC**: `55_probe_no_nav_prefill_hidden_qc.py` (+ `54_probe_no_nav_hidden_qc.py`) — the prefill KV/hidden the AE reads at the traj boundary. Extract per-layer KV change magnitude between ckpts (CE baseline as reference): ‖KV_x − KV_CE‖ / ‖KV_CE‖, cosine, per-layer.
- Optionally `66_eval_teacher_forced_traj_geometry_probe.py`, `68-70 hidden→action probe` for linear predictivity of action from hidden.

Deliverable: table over {CE, CE+KD, LoRA} × {hidden CKA-to-teacher, KV Δ-to-CE per layer, hidden→action probe R²}. Answers whether KD (and LoRA vs FullFT) changed the representation the AE actually consumes — the open thread from Report 145 §7. NOTE: KV-diff extraction likely needs a small custom dumper on top of scripts 55/50-51; build + verify before batch run.

---

## Execution notes
- GPU is single H200; 444K training holds it until completion. All above runs AFTER training frees GPU. Sequence heavy items (25/70/82 ~10-20 min each; hidden/KV probes small).
- Build order: ①+A (mechanical, extend `run_r1kd_200k_compare.sh`) → ② (health) → B/C (needs custom KV dumper, verify first).
